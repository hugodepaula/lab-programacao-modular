package app;


/**
*
* <p>Estrutura de um programa java básico.</p>
* <p>
* - Todo código da lógica do programa deve estar dentro de uma classe. <br />
* - O único código que é declarado externamente a uma classe corresponde às 
* declarações de pacotes (<code>package</code>) e às importações de pacotes 
* e classes (<code>import</code>).
* </p>
* 
* @author Prof. Hugo de Paula
* 
*/
public class AloMundo {

	/**
	 * <p>Toda aplicação se inicia pelo método <code>main</code>.</p> <br />
	 * 
	 * O método <code>main</code> deve ser público (<code>public</code>) para 
	 * ser visível externamente à classe, e deve ser estático  (<code>static</code>) 
	 * para ser acessível antes mesmo da criação de um objeto do tipo AloMundo.
	 * 
	 * @param args - número inteiro positivo que representa a quantidade de raios que
	 *            se deseja calcular
	 *
	 */
	public static void main(String[] args) {
		
		System.out.println("Bem vindos à PUC Minas!");

	}

}
